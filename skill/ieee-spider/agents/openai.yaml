interface:
  display_name: "IEEE Spider"
  short_description: "IEEE collections, citation stats, DOCX summaries"
  default_prompt: "Use $ieee-spider for IEEE Xplore search, per-author paper collections, manifest-driven downloads, serial abstract/citation enrichment, fixed venue statistics, and DOCX paper summaries. Require a human to complete the initial login; never run parallel IEEE browser pages, fall back to another metadata source, or use a proxy."
